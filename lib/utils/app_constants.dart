enum Category {
  business,
  entertainment,
  environment,
  food,
  health,
  politics,
  science,
  sports,
  technology,
  top,
  world,
}

enum Language { english, hindi }

enum ApiMethod { post, get, put, delete }

enum ApiStatus { success, error }
